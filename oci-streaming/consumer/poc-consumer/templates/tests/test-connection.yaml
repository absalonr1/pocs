apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "poc-consumer.fullname" . }}-test-connection"
  labels:
    {{- include "poc-consumer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "poc-consumer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
